# 테스트 모드 개발 진행 사항

테스트 모드 기능의 구현 현황과 단계별 남은 작업을 정리한 문서입니다.  
설계 기준: **`docs/테스트_모드_설계.md`**

본 문서의 최우선 원칙:

- 테스트 모드는 **로컬 테스트 전용 기능**이다.
- 테스트 모드로 인해 **운영 로직/운영 데이터/운영 화면 동작이 바뀌면 안 된다**.
- 테스트 모드 OFF 시 실행 경로는 기존과 **완전히 동일**해야 한다.

## 목적/진행 프로세스 (자연어)

이 개발 진행 사항의 목적은 운영 기능을 바꾸는 것이 아니라, 개발/QA가 로컬 환경에서 다양한 상황을 빠르게 재현해 검증할 수 있게 하는 데 있다.  
즉, 서버 시간이 맞지 않거나 특정 정책 응답을 받기 어려운 상황에서도, 테스트 모드에서 값만 바꿔 시업/종업/이석/임시연장/긴급사용/긴급해제 흐름을 반복 검증할 수 있어야 한다.

진행 프로세스는 다음과 같다.

1. 운영 로직과 테스트 로직을 먼저 분리한다. (테스트 모드 OFF 시 기존 코드와 동일 동작 보장)
2. 테스트 모드 상태를 `test-mode-state.json`에만 저장해 운영 상태 파일과 완전히 분리한다.
3. 화면 타입/동작 모드 강제값을 적용해 핵심 시나리오를 즉시 재현 가능하게 만든다.
4. Drawer에서 서버 응답값을 임의로 입력·적용하고, 프리셋으로 반복 테스트를 단순화한다.
5. `WorkTimeResponse` 전 필드 커버리지를 문서로 추적해 “테스트 가능한 값”의 누락을 지속적으로 제거한다.
6. 마지막으로 운영 차단 장치(`allowTestMode`, 환경 조건)를 적용해 실운영 노출 위험을 막는다.

| 항목 | 내용 |
|------|------|
| 작성일 | 2026-02-27 |
| 관련 코드 | `app/main/index.ts`, `app/preload/index.ts`, `app/renderer/*`, `app/core/screen-display-logic.ts` |

---

## 1. 현재 완료된 항목

### Main Process (`app/main/index.ts`)

- [x] **테스트 모드 상태 변수**: `testModeEnabled`, `testModeOverrides` (메모리)
- [x] **override 병합**: `mergeTestOverrides(base)` — base에 overrides 병합 후 반환
- [x] **getWorkTime 테스트 경로**: `testModeEnabled && overrides 있음` 시 API 우회, `mergeTestOverrides(base)` 결과 반환, 로컬 이석 감지 시 `screenType=empty` 반영
- [x] **IPC 핸들러**: `pcoff:getTestModeState`, `pcoff:setTestModeEnabled`, `pcoff:setTestOverrides`
- [~] **테스트 모드 변경 브로드캐스트(부분)**: `enabled` 변경 시만 `pcoff:test-mode-changed` 전송, `setTestOverrides` 변경 브로드캐스트는 미구현
- [x] **setTestOverrides 시 lastWorkTimeData 갱신**: overrides 적용 후 `lastWorkTimeData`·`lastWorkTimeFetchedAt` 갱신

### 미완료(추가 필요)

- [ ] **forceScreenType**: `AUTO | BEFORE | OFF | EMPTY` 강제 적용 로직
- [ ] **forceOperationMode**: `AUTO | NORMAL | TEMP_EXTEND | EMERGENCY_USE | EMERGENCY_RELEASE` 강제 적용 로직
- [ ] **핫키**: `Ctrl+Shift+T` / `Cmd+Shift+T` 등록·해제
- [ ] **설정 저장/복원**: `test-mode-state.json` 읽기/쓰기 (운영 `state.json` 분리 필수)
- [ ] **bypassApi 옵션**: testMode.bypassApi 플래그 및 동작
- [ ] **applyTestPreset**: `pcoff:applyTestPreset` 핸들러 및 프리셋 정의
- [ ] **테스트 모드 전용 로그**: TEST_MODE_ENABLED, TEST_MODE_DISABLED 등 (logcode.md 반영)
- [ ] **운영 차단 장치**: `config.allowTestMode` + 환경 조건(예: dev/qa build)으로 운영 배포에서 기본 차단

### Preload (`app/preload/index.ts`)

- [ ] **Renderer 노출 API**: `getTestModeState()`, `setTestModeState(patch)`, `applyTestPreset(name)`, `onTestModeChanged(handler)` — 현재 미노출

### Renderer

- [ ] **테스트 모드 토글 버튼**: 에이전트 화면 상단 우측
- [ ] **우측 Drawer (Test Mode Panel)**: ON 시 자동 오픈, OFF 시 닫힘
- [ ] **Drawer 내 입력·프리셋**: 설계서 §5 섹션별 UI

---

## 2. 단계별 남은 작업

### 2.1 1단계 (우선)

| # | 작업 | 담당 레이어 | 상세 |
|---|------|-------------|------|
| 1 | **forceScreenType 적용** | Main | `getWorkTime`뿐 아니라 `isLockRequired`/주기 잠금 검사/잠금창 초기 데이터 경로까지 동일하게 강제 적용. AUTO면 기존 운영 계산 사용. |
| 2 | **forceOperationMode 적용** | Main | 테스트 모드에서만 `currentMode` 조회/판단 경로를 강제. 적용 대상: `getOperationMode`, `isLockRequired`, 버튼/행동 분기에서 참조하는 모드 값. AUTO면 기존 운영 모드 사용. |
| 3 | **테스트 모드 설정 저장/복원** | Main | 운영 상태 파일과 분리된 `test-mode-state.json`에만 저장. 로그인/로그아웃/운영 상태 초기화와 독립적으로 관리. |
| 4 | **핫키 등록** | Main | `Ctrl+Shift+T`(Win) / `Cmd+Shift+T`(Mac). app ready 후 등록, before-quit에서 unregister. 동작: 테스트 모드 토글 + 필요 시 창 포커스/브로드캐스트. |
| 5 | **Preload 테스트 모드 API 노출** | Preload | `getTestModeState`, `setTestModeState(patch)`, `onTestModeChanged(handler)` 추가. `applyTestPreset`은 2단계에서 추가 가능. |
| 6 | **테스트 모드 ON/OFF 토글 UI** | Renderer | 작동정보(main.html) 상단 우측 토글. ON 시 Drawer 오픈(2단계에서 Drawer 구현 시 연동). 운영 배포 노출 정책(숨김/차단)과 함께 구현. |

**1단계 완료 기준**: 핫키 또는 토글로 테스트 모드 ON/OFF 가능, forceScreenType으로 시업/종업/이석 화면 즉시 재현, 재시작 후 설정 복원.

---

### 2.2 2단계

| # | 작업 | 담당 레이어 | 상세 |
|---|------|-------------|------|
| 7 | **우측 Drawer UI** | Renderer | Test Mode Panel. ON 시 표시, OFF 시 숨김. 기본·시간/근태·정책·프리셋 섹션 구조. |
| 8 | **기본 섹션** | Renderer | 테스트 모드 ON/OFF, API 우회 ON/OFF, 강제 화면 타입(AUTO/BEFORE/OFF/EMPTY), **강제 동작 모드(AUTO/NORMAL/TEMP_EXTEND/EMERGENCY_USE/EMERGENCY_RELEASE)** 선택. |
| 9 | **시간/근태·정책 입력** | Renderer | pcOnYmdTime, pcOffYmdTime, pcExCount/Max/Time, exCountRenewal, leaveSeat* 등. 입력 검증(시간 12자리, 숫자 범위). 적용 시 setTestOverrides 호출. |
| 10 | **applyTestPreset** | Main + Preload + Renderer | Main: `pcoff:applyTestPreset` 핸들러. 프리셋: 시업 잠금 재현, 종업 잠금 재현, 이석(사유필수/사유면제), **모드 강제 프리셋(일반/임시연장/긴급사용/긴급해제)**, 초기화. Preload/Renderer에서 호출·프리셋 버튼 노출. |
| 11 | **즉시 반영** | Renderer | 값 변경·프리셋 적용 후 getWorkTime/getOperationMode 재조회 또는 이벤트로 잠금 상태/버튼 가시성/문구·모드 배지 갱신. |

**2단계 완료 기준**: Drawer에서 주요 값 수정·프리셋 1회 클릭으로 시업/종업/이석 각각 재현, 적용 즉시 화면 반영.

---

### 2.3 3단계

| # | 작업 | 담당 레이어 | 상세 |
|---|------|-------------|------|
| 12 | **잠금화면 문구/리소스 override** | Main + Renderer | before/off/leave 별 title, message, backgroundUrl, logoUrl. 설정 저장·getWorkTime 또는 별도 채널로 전달. |
| 13 | **테스트 모드 로그 고도화** | Main | TEST_MODE_ENABLED, TEST_MODE_DISABLED, TEST_MODE_OVERRIDE_UPDATED, TEST_MODE_PRESET_APPLIED, TEST_MODE_FORCE_SCREEN_CHANGED, **TEST_MODE_FORCE_OPERATION_CHANGED**. logcode.md 반영. |
| 14 | **비개발자 테스트 가이드** | docs | 테스트 모드 사용법, 프리셋 설명, 주의사항(테스트 모드 ON 배너 인지). |
| 15 | **allowTestMode·트레이 노출** | Main | config.json의 `allowTestMode: false` 시 테스트 모드 비활성. 트레이 메뉴 `개발/테스트` 섹션에서만 테스트 모드 진입 노출(선택). |

**3단계 완료 기준**: 문구/이미지 override 가능, 로그·가이드 정리, 필요 시 운영 배포에서 테스트 모드 접근 제어.

---

## 3. 수용 기준 체크리스트 (설계서 §10)

| # | 기준 | 상태 |
|---|------|------|
| AC1 | 핫키 또는 UI 버튼으로 테스트 모드 ON/OFF 가능 | ⬜ |
| AC2 | 우측 Drawer에서 주요 값 수정 후 즉시 반영 | ⬜ |
| AC3 | 프리셋 1회 클릭으로 시업/종업/이석 화면 각각 재현 | ⬜ |
| AC3-1 | 프리셋/선택으로 일반/임시연장/긴급사용/긴급해제 모드 강제 재현 가능 | ⬜ |
| AC4 | 앱 재시작 후 테스트 모드 설정 복원 | ⬜ |
| AC5 | 테스트 모드 OFF 시 운영 로직과 결과 동일 | ⬜ |
| AC6 | `WorkTimeResponse` 전 필드에 대해 테스트 모드 override 가능 여부가 문서로 추적됨 | ⬜ |

---

## 4. 데이터 모델 (설계서 §6 기준)

저장 대상 예시 (`test-mode-state.json` 고정):

```json
{
  "testMode": {
    "enabled": false,
    "bypassApi": true,
    "forceScreenType": "AUTO",
    "forceOperationMode": "AUTO",
    "overrides": {
      "pcOnYmdTime": "202602270830",
      "pcOffYmdTime": "202602271830",
      "pcExCount": 0,
      "pcExMaxCount": 3,
      "pcExTime": 30,
      "leaveSeatUseYn": "Y",
      "leaveSeatTime": 10,
      "leaveSeatReasonYn": "YES",
      "leaveSeatReasonManYn": "YES",
      "leaveSeatUnlockRequirePassword": true
    }
  }
}
```

- **1단계**: `enabled`, `forceScreenType`, `forceOperationMode`, (선택) `overrides` 저장/복원.
- **2단계**: `bypassApi`, 전체 overrides 키 확장.
- **3단계**: 잠금화면 문구/리소스 override 키 추가 시 스키마 확장.

주의:

- 운영 파일(`state.json`, `config.json`의 로그인/운영 필드)과 교차 저장 금지.
- 테스트 모드 OFF 시에도 마지막 테스트 세트는 `test-mode-state.json`에만 유지(빠른 재시험용), 운영 로직에는 미반영.

### 4.1 서버값 전항목 커버리지 체크리스트 (필수 추가)

대상 스키마:

- `app/core/api-client.ts`의 `WorkTimeResponse` 전 필드

운영 규칙:

- `WorkTimeResponse`에 필드가 추가/변경되면, 아래 표도 같은 커밋에서 반드시 업데이트한다.
- 표의 `Override 가능`이 `N`인 항목은 사유와 대응 계획을 반드시 적는다.

체크리스트(초안 템플릿):

| 필드명 | 사용 경로(Main/Renderer) | 테스트 모드 Override 가능 | UI 입력 제공 | 검증 시나리오 | 비고 |
|---|---|---|---|---|---|
| `pcOnYmdTime` | `getWorkTime`, `isLockRequired`, 잠금 UI | Y | Y | 시업/종업 경계 테스트 |  |
| `pcOffYmdTime` | `getWorkTime`, `isLockRequired`, 잠금 UI | Y | Y | 시업/종업 경계 테스트 |  |
| `pcExCount` | 잠금 버튼 가시성/임시연장 | Y | Y | 임시연장 소진/잔여 |  |
| `pcExMaxCount` | 잠금 버튼 가시성/임시연장 | Y | Y | 임시연장 소진/잔여 |  |
| `pcExTime` | 임시연장 처리/안내 | Y | Y | 연장 시간 반영 |  |
| `pcoffEmergencyYesNo` | 긴급사용 버튼/흐름 | Y | Y | 긴급사용 가능/불가 |  |
| `emergencyUseYesNo` | 긴급사용 승인 상태 | Y | (선택) | 긴급사용 분기 |  |
| `emergencyStaDate` | 긴급사용 시간창 | Y | (선택) | 긴급사용 시간 판단 |  |
| `emergencyEndDate` | `isLockRequired`(긴급사용 종료) | Y | (선택) | 긴급사용 종료 복귀 |  |
| `leaveSeatUseYn` | 이석 감지 정책 | Y | Y | 이석 감지 ON/OFF |  |
| `leaveSeatTime` | 이석 감지 임계시간 | Y | Y | Idle/Sleep 이석 발생 |  |
| `leaveSeatReasonYn` | 이석 해제 사유 분기 | Y | Y | 사유 입력 표시 |  |
| `leaveSeatReasonManYn` | 이석 사유 필수 분기 | Y | Y | 사유 필수/면제 |  |
| `leaveSeatUnlockRequirePassword` | 이석 해제 비번 모달 | Y | Y | 비번 모달 표시/미표시 |  |
| `screenType` | 잠금 타입(before/off/empty) | Y | Y(forceScreenType) | 화면 강제 전환 |  |
| `lockScreen*` 계열 | 잠금 제목/문구/이미지 | Y | 3단계 | 잠금 화면 렌더링 |  |
| `emergencyUnlock*` 계열 | 긴급해제 정책/횟수/차단 | Y | (선택) | 긴급해제 제한 정책 |  |
| `기타 필드` | 미사용 또는 로그/표시용 | TBD | TBD | TBD | 사용처 확인 후 확정 |

완료 기준:

- 위 표의 `TBD` 제거
- `Override 가능=N` 0건 또는 예외 사유 승인
- CI 또는 수동 체크리스트에서 핵심 필드 시나리오 PASS 기록

---

## 5. 리스크 및 대응 (설계서 §12)

| 리스크 | 대응 |
|--------|------|
| 테스트 모드 코드가 운영 경로 오염 | `if (!testModeEnabled) return ...` 조기 분기 유지, 테스트 전용 변수·분기만 사용 |
| 테스트 모드가 운영 잠금 판단을 부분적으로만 덮어써 동작 불일치 발생 | `getWorkTime`, `isLockRequired`, 잠금창 초기 데이터, 주기 체크 경로를 동일 규칙으로 통합 적용 |
| 테스트 모드에서 동작 모드 강제가 일부 경로에만 반영되어 버튼/상태 표기가 어긋남 | `getOperationMode`, mode-changed 이벤트, UI 배지/버튼 가시성 판단 경로를 동일 강제 규칙으로 통합 |
| 잘못된 입력으로 UI 깨짐 | Drawer 입력 검증(시간 12자리, 숫자 범위), 유효성 실패 시 적용 차단 |
| QA가 테스트 모드 상태를 잊고 운영 테스트 수행 | 상단 고정 배너, 트레이 상태 표시, 테스트 모드 관련 로그 기록 |
| 운영 배포에서 테스트 모드 노출 | `allowTestMode=false` 기본값 + 빌드 타입/환경 플래그로 접근 차단 |

---

## 6. 참고

- **설계서**: `docs/테스트_모드_설계.md`
- **화면 타입 로직**: `app/core/screen-display-logic.ts` — `resolveScreenType`
- **현재 Main 테스트 모드**: `app/main/index.ts` — `testModeEnabled`, `testModeOverrides`, `mergeTestOverrides`, `pcoff:getWorkTime` 내 분기
